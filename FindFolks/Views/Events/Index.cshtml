@model FindFolks.Models.EventsIndexModel

@{
    ViewBag.Title = "Events";
}

<h2>Events</h2>
<hr />

<div class="row">
    <div class="col-md-4">
        <div class="panel panel-info">
            <div class="panel-heading">
                <div class="panel-title">Events you're supposed to be at now</div>
            </div>
            @if (Model.UserCurrentEvents != null && Model.UserCurrentEvents.Count() != 0)
            {
                <div class="list-group">
                    @foreach (var e in Model.UserCurrentEvents)
                    {
                        <a href="@Url.Action("Info", "Events", new { Id = e.Id })" class="list-group-item">
                            <h4 class="list-group-item-heading">@e.Title <small>#@e.Id</small><span class="pull-right"><i class="fa fa-angle-right fa-fw"></i></span></h4>
                            <p class="list-group-item-text"><i class="fa fa-calendar fa-fw"></i> @e.Start.ToString()</p>
                            <p class="list-group-item-text"><i class="fa fa-info-circle fa-fw"></i> @e.Description</p>
                            <p class="list-group-item-text"><i class="fa fa-map-pin fa-fw"></i> @e.Location in @e.ZipCode</p>
                            <p class="list-group-item-text"><i class="fa fa-group fa-fw"></i> @e.GroupName</p>
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="panel-body">
                    <p>Looks like nothing's in progress right now.</p>
                </div>
            }
        </div>
    </div>
    <div class="col-md-4">
        <div class="panel panel-info">
            <div class="panel-heading">
                <h3 class="panel-title">Your upcoming events</h3>
            </div>
            @if (Model.UserUpcomingEvents != null && Model.UserUpcomingEvents.Count() != 0)
            {
                <div class="list-group">
                    @foreach (var e in Model.UserUpcomingEvents)
                    {
                        <a href="@Url.Action("Info", "Events", new { Id = e.Id })" class="list-group-item">
                            <h4 class="list-group-item-heading">@e.Title <small>#@e.Id</small><span class="pull-right"><i class="fa fa-angle-right fa-fw"></i></span></h4>
                            <p class="list-group-item-text"><i class="fa fa-calendar fa-fw"></i> @e.Start.ToString()</p>
                            <p class="list-group-item-text"><i class="fa fa-info-circle fa-fw"></i> @e.Description</p>
                            <p class="list-group-item-text"><i class="fa fa-map-pin fa-fw"></i> @e.Location in @e.ZipCode</p>
                            <p class="list-group-item-text"><i class="fa fa-group fa-fw"></i> @e.GroupName</p>
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="panel-body">
                    <p>You don't have any events coming up!</p>
                </div>
            }
        </div>
    </div>
    <div class="col-md-4">
        <div class="panel panel-info">
            <div class="panel-heading">
                <h3 class="panel-title">Events you might find interesting</h3>
            </div>
            @if (Model.UserInterestedEvents != null && Model.UserInterestedEvents.Count() != 0)
            {
                <div class="list-group">
                    @foreach (var e in Model.UserInterestedEvents)
                    {
                        <a href="@Url.Action("Info", "Events", new { Id = e.Id })" class="list-group-item">
                            <h4 class="list-group-item-heading">@e.Title <small>#@e.Id</small><span class="pull-right"><i class="fa fa-angle-right fa-fw"></i></span></h4>
                            <p class="list-group-item-text"><i class="fa fa-calendar fa-fw"></i> @e.Start.ToString()</p>
                            <p class="list-group-item-text"><i class="fa fa-info-circle fa-fw"></i> @e.Description</p>
                            <p class="list-group-item-text"><i class="fa fa-map-pin fa-fw"></i> @e.Location in @e.ZipCode</p>
                            <p class="list-group-item-text"><i class="fa fa-group fa-fw"></i> @e.GroupName</p>
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="panel-body">
                    <p>Sorry, nothing was found!</p>
                </div>
            }
        </div>
    </div>
</div>
<hr />
<div class="row">
    <div class="col-md-6">
        <div class="panel panel-info">
            <div class="panel-heading">
                <h3 class="panel-title">All things happening right now</h3>
            </div>
            @if (Model.AllHappeningNow != null && Model.AllHappeningNow.Count() != 0)
            {
                <div class="list-group">
                    @foreach (var e in Model.AllHappeningNow)
                    {
                        <a href="@Url.Action("Info", "Events", new { Id = e.Id })" class="list-group-item">
                            <h4 class="list-group-item-heading">@e.Title <small>#@e.Id</small><span class="pull-right"><i class="fa fa-angle-right fa-fw"></i></span></h4>
                            <p class="list-group-item-text"><i class="fa fa-calendar fa-fw"></i> @e.Start.ToString()</p>
                            <p class="list-group-item-text"><i class="fa fa-info-circle fa-fw"></i> @e.Description</p>
                            <p class="list-group-item-text"><i class="fa fa-map-pin fa-fw"></i> @e.Location in @e.ZipCode</p>
                            <p class="list-group-item-text"><i class="fa fa-group fa-fw"></i> @e.GroupName</p>
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="panel-body">
                    <p>Looks like it's silent out there!</p>
                </div>
            }
        </div>
    </div>
    <div class="col-md-6">
        <div class="panel panel-info">
            <div class="panel-heading">
                <h3 class="panel-title">All upcoming events globally</h3>
            </div>
            @if (Model.AllUpcomingEvents != null && Model.AllUpcomingEvents.Count() != 0)
            {
                <div class="list-group">
                    @foreach (var e in Model.AllUpcomingEvents)
                    {
                        <a href="@Url.Action("Info", "Events", new { Id = e.Id })" class="list-group-item">
                            <h4 class="list-group-item-heading">@e.Title <small>#@e.Id</small><span class="pull-right"><i class="fa fa-angle-right fa-fw"></i></span></h4>
                            <p class="list-group-item-text"><i class="fa fa-calendar fa-fw"></i> @e.Start.ToString()</p>
                            <p class="list-group-item-text"><i class="fa fa-info-circle fa-fw"></i> @e.Description</p>
                            <p class="list-group-item-text"><i class="fa fa-map-pin fa-fw"></i> @e.Location in @e.ZipCode</p>
                            <p class="list-group-item-text"><i class="fa fa-group fa-fw"></i> @e.GroupName</p>
                        </a>
                    }
                </div>
            }
            else
            {
                <div class="panel-body">
                    <p>Seems like no events are scheduled at all!</p>
                </div>
            }
        </div>
    </div>
</div>